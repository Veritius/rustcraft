[package]
name = "rustcraft_server"
version = "0.1.0"
edition = "2021"
authors = ["Veritius"]

[dependencies]
rustcraft_shared = { path = "../shared" }
naia-bevy-server = { version = "0.10.1", features = ["use-udp"] }
naia-shared = "0.10.0"
bevy_proto = "0.4.0"